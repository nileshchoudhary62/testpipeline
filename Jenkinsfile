#!/bin/groovy
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {
                sh '''
                echo "hello"
                '''
                } }
        }
    }
}
